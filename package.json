{
  "name": "json-table-viewer-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "yarn workspace json-table-viewer dev",
    "dev:server": "yarn workspace server dev",
    "build": "yarn workspace json-table-viewer build",
    "build:server": "yarn workspace server build",
    "build:all": "yarn workspace json-table-viewer build && yarn workspace server build",
    "preview": "yarn workspace json-table-viewer preview"
  }
}
